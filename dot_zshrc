# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/Users/caryz/miniforge3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/Users/caryz/miniforge3/etc/profile.d/conda.sh" ]; then
        . "/Users/caryz/miniforge3/etc/profile.d/conda.sh"
    else
        export PATH="/Users/caryz/miniforge3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<

export PATH="/opt/homebrew/opt/openjdk/bin:$PATH"
alias reload='source ~/.zshrc'
alias tailscale='/Applications/Tailscale.app/Contents/MacOS/Tailscale'
alias n8nstart='/Users/caryz/.scripts/start-n8n.sh'
alias proxy='. /Users/caryz/.scripts/proxy.sh'
alias cz='chezmoi'
alias co='open -a Cursor'
alias wakeonlan='/opt/homebrew/bin/wakeonlan B0:25:AA:4D:71:A0'
alias wakeup="ssh macmini zsh -ic wakeonlan"
export HOMEBREW_BREW_GIT_REMOTE="https://mirrors.tuna.tsinghua.edu.cn/git/homebrew/brew.git"
export HOMEBREW_CORE_GIT_REMOTE="https://mirrors.tuna.tsinghua.edu.cn/git/homebrew/homebrew-core.git"
export HOMEBREW_API_DOMAIN="https://mirrors.tuna.tsinghua.edu.cn/homebrew-bottles/api"
export HOMEBREW_BOTTLE_DOMAIN="https://mirrors.tuna.tsinghua.edu.cn/homebrew-bottles"
export PATH="$HOME/.jenv/bin:$PATH"
eval "$(jenv init -)"

# Added by Qoder CLI installer
export PATH="$PATH:/Users/caryz/.local/bin"
eval "$(mise activate zsh)"

# proxy switch
proxy_on () {
  export HTTP_PROXY=http://100.77.228.65:7890
  export HTTPS_PROXY=http://100.77.228.65:7890
  export ALL_PROXY=socks5://100.77.228.65:7892
}

proxy_off () {
  unset HTTP_PROXY HTTPS_PROXY ALL_PROXY
}

proxy_test () {
  curl https://www.google.com
}

czpush() {
  cd "$(chezmoi source-path)" || return
  git add .
  git commit -m "update"
  git push
}

cozsh() {
  cursor -nw ~/.zshrc
  echo "Please save the file and source it"
  source ~/.zshrc
}

# >>> mamba initialize >>>
# !! Contents within this block are managed by 'mamba shell init' !!
export MAMBA_EXE='/Users/caryz/miniforge3/bin/mamba';
export MAMBA_ROOT_PREFIX='/Users/caryz/miniforge3';
__mamba_setup="$("$MAMBA_EXE" shell hook --shell zsh --root-prefix "$MAMBA_ROOT_PREFIX" 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__mamba_setup"
else
    alias mamba="$MAMBA_EXE"  # Fallback on help from mamba activate
fi
unset __mamba_setup
# <<< mamba initialize <<<

# edit command line
autoload -Uz edit-command-line
zle -N edit-command-line
bindkey '^E' edit-command-line
export EDITOR="cursor -nw"
export VISUAL="$EDITOR"